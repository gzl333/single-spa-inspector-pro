node_modules
build
web-ext-artifacts
# prefer package-lock.json
yarn.lock
# "Keep it secret. Keep it safe." - Gandalf the Grey
.env
.env.local
!.env.example

# dist folder for Chrome build (generated)
dist-chrome/build